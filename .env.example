# Not required for Phase 1 (this scaffold). Documented here for the phases
# that follow, so the env surface is visible up front.

# Backend (Phase 3+): Firebase Admin SDK project + admin allowlist
FIREBASE_PROJECT_ID=
ADMIN_EMAILS=

# Backend (Phase 4): YouTube Data API sync
YOUTUBE_API_KEY=
YOUTUBE_CHANNEL_ID=

# Frontend (Phase 3+): Firebase web config (public, safe to bake into the build)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
