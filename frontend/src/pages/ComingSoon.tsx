export default function ComingSoon({ title }: { title: string }) {
  return (
    <main className="coming-soon">
      <h1>{title}</h1>
      <p>This part of SafeSurf is under construction — check back soon.</p>
    </main>
  )
}
